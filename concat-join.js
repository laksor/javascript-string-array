// concat

const first = 'My';
const second = 'City';

const result = first.concat(second);

console.log(result);

// join

const words = ['a', 'b', 'c', 'd'];
const joined = words.join(', ');
// const joined = words.join(' ');

console.log(joined);